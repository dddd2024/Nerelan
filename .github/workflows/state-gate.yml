name: State Gate

on:
  push:
    branches:
      - "main"
  pull_request_target:
    types:
      - opened
      - edited
      - synchronize
      - reopened
      - ready_for_review
      - converted_to_draft
      - labeled
      - unlabeled
      - auto_merge_enabled
      - auto_merge_disabled

permissions:
  contents: read
  actions: read
  pull-requests: read
  issues: read

jobs:
  # ===========================================================================
  # Trusted authority verification job
  #
  # Uses base-branch code for all authority checks.  Candidate code never
  # executes before the authority gate passes.  No GITHUB_TOKEN is passed to
  # candidate code.
  # ===========================================================================
  authority-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    outputs:
      mode: ${{ steps.control_plane.outputs.mode }}
      path_a_authorized: ${{ steps.path_a_authority.outputs.authorized }}
    steps:
      - name: Checkout trusted base
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.base.sha || github.sha }}
          persist-credentials: false

      - name: Extract candidate project_state to isolated directory
        if: github.event_name == 'pull_request_target'
        id: candidate_state
        shell: bash
        run: |
          git fetch origin "${{ github.event.pull_request.head.sha }}"
          # Extract candidate project_state to an isolated directory.
          # Never overlay candidate authority files onto the trusted workspace.
          mkdir -p "$RUNNER_TEMP/candidate_state"
          git archive "${{ github.event.pull_request.head.sha }}" -- project_state/ | tar -x -C "$RUNNER_TEMP/candidate_state"
          echo "state_dir=$RUNNER_TEMP/candidate_state/project_state" >> "$GITHUB_OUTPUT"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install trusted package
        run: python -m pip install -e ".[test]"

      - name: Build trusted route observation (trusted base, no candidate checkout)
        id: control_plane
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" != "pull_request_target" ]; then
            # push to main: use existing transition/mainline logic
            echo "mode=transition" >> "$GITHUB_OUTPUT"
            echo "changed_paths_sha256=" >> "$GITHUB_OUTPUT"
            echo "trusted_checkout_sha=${{ github.sha }}" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          # Fetch the candidate Git object so the trusted base checkout can
          # diff against it WITHOUT checking it out.
          git fetch origin "${{ github.event.pull_request.head.sha }}"
          python -m reverse_agent.project_gate control-plane-mode --state-dir project_state \
            --event-path "$GITHUB_EVENT_PATH" \
            --output "$RUNNER_TEMP/trusted_route.json" \
            --repository "$GITHUB_REPOSITORY"

      - name: Load candidate authority from isolated directory
        if: github.event_name == 'pull_request_target' && steps.control_plane.outputs.mode == 'transition'
        shell: bash
        run: |
          # Copy candidate project_state from the isolated directory into the
          # working tree for transition validation.  This is NOT
          # ``git checkout candidate -- project_state/``; the candidate state
          # was already extracted via ``git archive`` into an isolated dir.
          # Mode detection already ran against the trusted base's project_state
          # and the trusted route observation.  Candidate project_state is only
          # read AFTER mode is determined to be transition.
          if [ -d "$RUNNER_TEMP/candidate_state/project_state" ]; then
            rm -rf project_state
            cp -r "$RUNNER_TEMP/candidate_state/project_state" project_state
          fi

      # ----- Transition mode (trusted verifier) -----
      # Branch lifecycle routing (pull_request_target equivalent of
      # github.ref != 'refs/heads/main' from the original pull_request model):
      #   PR events  -> transition-preflight
      #   push main  -> integration-baseline, mainline-merge-validation, receipt
      - name: Transition lint
        if: steps.control_plane.outputs.mode == 'transition'
        run: python -m reverse_agent.project_gate transition-lint --state-dir project_state

      - name: Transition command plan
        if: steps.control_plane.outputs.mode == 'transition'
        run: python -m reverse_agent.project_gate transition-command-plan --state-dir project_state

      - name: Transition preflight
        if: steps.control_plane.outputs.mode == 'transition' && github.event_name == 'pull_request_target'
        run: python -m reverse_agent.project_gate transition-preflight --state-dir project_state

      - name: Historical integration baseline
        if: steps.control_plane.outputs.mode == 'transition' && github.event_name == 'push'
        run: python -m reverse_agent.project_gate integration-baseline --state-dir project_state

      - name: Current mainline merge validation
        if: steps.control_plane.outputs.mode == 'transition' && github.event_name == 'push'
        env:
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: python -m reverse_agent.project_gate mainline-merge-validation --state-dir project_state

      - name: Emit mainline integration receipt
        if: steps.control_plane.outputs.mode == 'transition' && github.event_name == 'push'
        env:
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: >-
          python -m reverse_agent.project_gate emit-mainline-integration-receipt
          --state-dir project_state
          --output "${{ runner.temp }}/mainline_integration_receipt.json"

      # ----- Legacy mode (trusted verifier) -----
      - name: Project gate preflight
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate preflight --state-dir project_state

      - name: Project gate command plan
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate command-plan --state-dir project_state

      - name: Project gate post-final evidence sync
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate post-final-evidence-sync --state-dir project_state

      - name: Project gate job lifecycle
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate job-lifecycle --state-dir project_state

      - name: Project gate audit inventory
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate audit-inventory --state-dir project_state

      - name: Project gate audit readiness packet
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate audit-readiness-packet --state-dir project_state

      - name: Project gate current handoff packet
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate current-handoff-packet --state-dir project_state

      - name: Project gate local execution bundle
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate local-execution-bundle --state-dir project_state

      - name: Project gate Codex prompt packet
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate codex-prompt-packet --state-dir project_state

      - name: Project gate audit precheck
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate audit-precheck --state-dir project_state

      - name: Project gate report summary
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate report-summary --state-dir project_state

      - name: Project gate execution log
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate execution-log --state-dir project_state

      - name: CI workflow coverage
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate ci-workflow-coverage --state-dir project_state

      - name: CI workflow readiness
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate ci-workflow-readiness --state-dir project_state

      - name: Decision preflight
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate decision-preflight --state-dir project_state

      - name: CI run evidence
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate ci-run-evidence --state-dir project_state

      - name: Local CI parity
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate local-ci-parity --state-dir project_state

      - name: CI observation schema
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate ci-observation-schema --state-dir project_state

      - name: CI observation handoff
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate ci-observation-handoff --state-dir project_state

      - name: CI observation reconcile
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate ci-observation-reconcile --state-dir project_state

      - name: CI artifact manifest
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate ci-artifact-manifest --state-dir project_state

      - name: CI audit handoff bundle
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate ci-audit-handoff-bundle --state-dir project_state

      - name: Project gate final check (legacy)
        if: steps.control_plane.outputs.mode == 'legacy'
        run: python -m reverse_agent.project_gate final-check --state-dir project_state

      # ----- Path-A R1 authority verification (trusted, live re-observation) -----
      - name: Path-A R1 collect GitHub state (live re-observation)
        if: steps.control_plane.outputs.mode == 'path_a_r1' && github.event_name == 'pull_request_target'
        id: path_a_state
        env:
          GH_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          # Parse issue number from PR body using trusted Python
          ISSUE_NUMBER=$(python -c "
          import json
          from reverse_agent.control_plane.path_a import parse_snapshot
          event = json.load(open('$GITHUB_EVENT_PATH'))
          pr = event.get('pull_request', {})
          snapshot = parse_snapshot(pr.get('body', ''))
          print(snapshot.issue_number)
          ")
          echo "issue_number=$ISSUE_NUMBER" >> "$GITHUB_OUTPUT"

          # Live re-query Issue scalars using GraphQL (includes lastEditedAt
          # for edit identity).  Labels are fetched separately via REST with
          # full pagination so the label set is complete.
          REPO_OWNER="${GITHUB_REPOSITORY%%/*}"
          REPO_NAME="${GITHUB_REPOSITORY#*/}"
          gh api graphql \
            -f owner="$REPO_OWNER" \
            -f name="$REPO_NAME" \
            -F number="$ISSUE_NUMBER" \
            -f query='query($owner:String!,$name:String!,$number:Int!){repository(owner:$owner,name:$name){issue(number:$number){number state body createdAt lastEditedAt}}}' \
            > "$RUNNER_TEMP/path_a_issue_graphql.json"

          # Complete label observation via REST with full pagination (--paginate --slurp)
          gh api "repos/$GITHUB_REPOSITORY/issues/$ISSUE_NUMBER/labels" \
            --paginate --slurp \
            > "$RUNNER_TEMP/path_a_labels_rest.json"

          # Normalize the GraphQL envelope (proper data.repository.issue unwrap)
          # and bind REST labels.  Fails closed on any malformation.
          python -c "
          import json, sys
          from reverse_agent.control_plane.path_a import normalize_issue_graphql_envelope
          with open('$RUNNER_TEMP/path_a_issue_graphql.json') as f:
              raw = json.load(f)
          with open('$RUNNER_TEMP/path_a_labels_rest.json') as f:
              labels_rest = json.load(f)
          normalized = normalize_issue_graphql_envelope(raw, labels_rest_pages=labels_rest)
          json.dump(normalized, open('$RUNNER_TEMP/path_a_issue.json', 'w'), ensure_ascii=False, sort_keys=True)
          "

          # Paginated events with --slurp for deterministic array-of-pages
          gh api "repos/$GITHUB_REPOSITORY/issues/$ISSUE_NUMBER/events" \
            --paginate --slurp \
            > "$RUNNER_TEMP/path_a_approval_events.json"

          # Live re-query PR state
          PR_NUMBER="${{ github.event.pull_request.number }}"
          gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" \
            --json number,state,isDraft,body,autoMergeRequest,headRefName,headRefOid,baseRefName,baseRefOid \
            > "$RUNNER_TEMP/path_a_pr.json"

          # Merge live PR data into event for authority verification
          python -c "
          import json
          event = json.load(open('$GITHUB_EVENT_PATH'))
          live_pr = json.load(open('$RUNNER_TEMP/path_a_pr.json'))
          event['pull_request'].update({
              'number': live_pr.get('number', event['pull_request'].get('number')),
              'state': live_pr.get('state', event['pull_request'].get('state')),
              'draft': live_pr.get('isDraft', event['pull_request'].get('draft')),
              'body': live_pr.get('body', event['pull_request'].get('body')),
              'auto_merge': live_pr.get('autoMergeRequest', event['pull_request'].get('auto_merge')),
              'head': {
                  'ref': live_pr.get('headRefName', event['pull_request']['head']['ref']),
                  'sha': live_pr.get('headRefOid', event['pull_request']['head']['sha']),
              },
              'base': {
                  'ref': live_pr.get('baseRefName', event['pull_request']['base']['ref']),
                  'sha': live_pr.get('baseRefOid', event['pull_request']['base']['sha']),
              },
          })
          json.dump(event, open('$RUNNER_TEMP/path_a_event.json', 'w'))
          "

          # Extract approver using trusted Python with flatten_paginated_events
          APPROVER=$(python -c "
          import json
          from reverse_agent.control_plane.path_a import flatten_paginated_events
          with open('$RUNNER_TEMP/path_a_approval_events.json') as f:
              raw = json.load(f)
          events = flatten_paginated_events(raw)
          transitions = [
              e for e in events
              if e.get('event') in ('labeled', 'unlabeled')
              and (e.get('label') or {}).get('name', '').lower() == 'r1-approved'
          ]
          transitions.sort(key=lambda e: (str(e.get('created_at', '')), int(e.get('id', 0))))
          effective = transitions[-1] if transitions else {}
          print((effective.get('actor') or {}).get('login', ''))
          ")
          if [ -z "$APPROVER" ]; then
            echo "path-a-r1: no r1-approved label event found on issue $ISSUE_NUMBER" >&2
            exit 1
          fi
          echo "approver=$APPROVER" >> "$GITHUB_OUTPUT"
          PERMISSION=$(gh api "repos/$GITHUB_REPOSITORY/collaborators/$APPROVER/permission" \
            --jq '.permission')
          echo "permission=$PERMISSION" >> "$GITHUB_OUTPUT"

      - name: Path-A R1 authority gate (trusted verifier)
        if: steps.control_plane.outputs.mode == 'path_a_r1' && github.event_name == 'pull_request_target'
        id: path_a_authority
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}
        run: |
          python -m reverse_agent.project_gate path-a-r1-gate \
            --event-path "$RUNNER_TEMP/path_a_event.json" \
            --issue-path "$RUNNER_TEMP/path_a_issue.json" \
            --approval-events-path "$RUNNER_TEMP/path_a_approval_events.json" \
            --approver-permission "${{ steps.path_a_state.outputs.permission }}" \
            --repository "$GITHUB_REPOSITORY" \
            --state-dir project_state \
            --changed-path-observation-path "$RUNNER_TEMP/trusted_route.json"
          echo "authorized=true" >> "$GITHUB_OUTPUT"

      - name: Upload authority gate evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: authority-gate-evidence
          if-no-files-found: warn
          path: |
            project_state/gates/*.json
            ${{ runner.temp }}/path_a_*.json
            ${{ runner.temp }}/trusted_route.json
            ${{ runner.temp }}/mainline_integration_receipt.json

  # ===========================================================================
  # Candidate test job (tokenless, post-authority)
  #
  # Candidate code runs ONLY after the authority gate passes AND only for
  # pull_request_target events.  No GITHUB_TOKEN or GH_TOKEN is passed to
  # candidate steps.  Uses persist-credentials: false.  Push events skip this
  # job entirely.
  # ===========================================================================
  candidate-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    needs: authority-gate
    if: always() && github.event_name == 'pull_request_target' && needs.authority-gate.result == 'success'
    steps:
      - name: Checkout candidate head (tokenless)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install candidate package
        run: python -m pip install -e ".[test]"

      - name: Path-A R1 focused tests (tokenless candidate, post-authority)
        if: needs.authority-gate.outputs.mode == 'path_a_r1' && needs.authority-gate.outputs.path_a_authorized == 'true'
        run: python -m pytest tests/test_path_a_gate.py tests/test_control_plane_transition.py tests/test_planning_and_github_adapters.py -q

      - name: Transition focused tests (tokenless candidate)
        if: needs.authority-gate.outputs.mode == 'transition'
        run: python -m pytest tests/test_path_a_gate.py tests/test_control_plane_transition.py tests/test_planning_and_github_adapters.py -q

      - name: Legacy focused tests (tokenless candidate)
        if: needs.authority-gate.outputs.mode == 'legacy'
        run: python -m pytest tests/test_project_gate.py tests/test_project_reports.py tests/test_project_jobs.py tests/test_post_final_evidence_sync.py tests/test_decision_preflight.py tests/test_project_state.py -q

      - name: Upload candidate test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: candidate-test-evidence
          if-no-files-found: warn
          path: |
            project_state/gates/*.json
            project_state/pytest_result.txt

  # ===========================================================================
  # Finalize receipt job (trusted, no candidate code)
  #
  # Runs only for pull_request_target events (even if candidate-tests fail).
  # Does NOT execute candidate code.  Generates a verifiable receipt artifact
  # bound to PR and candidate head, then ENFORCES the receipt as a blocking
  # gate.  Push events skip this job entirely.  Candidate tests failure or
  # missing/malformed receipt => receipt final_gate_result = BLOCKED => job
  # failure.
  # ===========================================================================
  finalize-receipt:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [authority-gate, candidate-tests]
    if: always() && github.event_name == 'pull_request_target'
    steps:
      - name: Checkout trusted base (no candidate code)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.base.sha || github.sha }}
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install trusted package
        run: python -m pip install -e ".[test]"

      - name: Download authority gate evidence
        uses: actions/download-artifact@v4
        with:
          name: authority-gate-evidence
          path: ${{ runner.temp }}/authority-evidence

      - name: Generate State Gate receipt
        shell: bash
        run: |
          set -euo pipefail
          python -c "
          import hashlib, json, os, subprocess
          from datetime import datetime, timezone

          repo = os.environ['GITHUB_REPOSITORY']
          event_name = os.environ['GITHUB_EVENT_NAME']
          run_id = os.environ['GITHUB_RUN_ID']
          run_attempt = os.environ.get('GITHUB_RUN_ATTEMPT', '1')
          workflow_path = os.environ.get('GITHUB_WORKFLOW_REF', '.github/workflows/state-gate.yml')
          # Extract just the workflow path portion
          workflow_path = workflow_path.split('@')[0] if '@' in workflow_path else workflow_path

          event = json.load(open(os.environ['GITHUB_EVENT_PATH']))
          pr = event.get('pull_request', {})
          pr_number = pr.get('number', 0)
          candidate_head = pr.get('head', {}).get('sha', '')
          candidate_base = pr.get('base', {}).get('sha', '')
          trusted_base = candidate_base

          # Reuse the authority observation's changed_paths_sha256.  The
          # receipt must NOT recompute changed paths from a fresh git diff;
          # it binds directly to the trusted authority observation.
          authority_evidence_dir = os.path.join(os.environ['RUNNER_TEMP'], 'authority-evidence')
          trusted_route_path = os.path.join(authority_evidence_dir, 'trusted_route.json')
          if os.path.exists(trusted_route_path):
              with open(trusted_route_path) as f:
                  observation = json.load(f)
              changed_paths_sha = observation.get('changed_paths_sha256', '')
              observed_base = observation.get('base_sha', '')
              observed_head = observation.get('head_sha', '')
              trusted_checkout_sha = observation.get('trusted_checkout_sha', '')
              # Bind receipt to the authority observation, not the event.
              if observed_base:
                  candidate_base = observed_base
                  trusted_base = observed_base
              if observed_head:
                  candidate_head = observed_head
          else:
              # No route observation: receipt must be BLOCKED, not PASS.
              changed_paths_sha = ''
              trusted_checkout_sha = ''

          # Authority and candidate test results
          authority_result = 'SUCCESS' if '${{ needs.authority-gate.result }}' == 'success' else 'FAILED'
          candidate_tests_result = 'SUCCESS' if '${{ needs.candidate-tests.result }}' == 'success' else 'FAILED'
          selected_mode = '${{ needs.authority-gate.outputs.mode }}'

          # Trusted verifier tree SHA (current HEAD = trusted base checkout)
          trusted_verifier_tree = subprocess.check_output(
              ['git', 'rev-parse', 'HEAD'], text=True
          ).strip()

          # Final gate result.  Missing route observation => BLOCKED.
          if not changed_paths_sha:
              final_gate_result = 'BLOCKED'
          elif authority_result != 'SUCCESS':
              final_gate_result = 'BLOCKED'
          elif candidate_tests_result != 'SUCCESS':
              final_gate_result = 'BLOCKED'
          else:
              final_gate_result = 'PASS'

          # authority_revision binds to the trusted authority observation
          # (trusted checkout SHA), not a fresh git rev-parse.
          authority_revision = trusted_checkout_sha or trusted_verifier_tree

          receipt = {
              'schema_version': '0.1',
              'receipt_kind': 'state_gate',
              'repository': repo,
              'pr_number': pr_number,
              'workflow_path': workflow_path,
              'workflow_event': event_name,
              'workflow_run_id': int(run_id),
              'workflow_run_attempt': int(run_attempt),
              'trusted_base_sha': trusted_base,
              'trusted_verifier_tree_sha': trusted_verifier_tree,
              'candidate_head_sha': candidate_head,
              'candidate_base_sha': candidate_base,
              'changed_paths_sha256': changed_paths_sha,
              'selected_mode': selected_mode,
              'authority_identity': 'trusted_base_verifier',
              'authority_revision': authority_revision,
              'authority_result': authority_result,
              'candidate_tests_result': candidate_tests_result,
              'final_gate_result': final_gate_result,
              'generated_at': datetime.now(timezone.utc).isoformat(),
          }

          # Compute content_sha256 over the canonical receipt (excluding itself)
          canonical = json.dumps(receipt, ensure_ascii=False, sort_keys=True, separators=(',', ':'))
          receipt['content_sha256'] = hashlib.sha256(canonical.encode('utf-8')).hexdigest()

          out_path = os.path.join(os.environ['RUNNER_TEMP'], 'state_gate_receipt.json')
          with open(out_path, 'w') as f:
              json.dump(receipt, f, indent=2, sort_keys=True)
          print(f'Receipt written to {out_path}')
          print(f'final_gate_result={final_gate_result}')
          "

      - name: Upload State Gate receipt artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: state-gate-receipt-pr${{ github.event.pull_request.number }}-${{ github.event.pull_request.head.sha }}
          path: ${{ runner.temp }}/state_gate_receipt.json

      - name: Enforce receipt as blocking gate
        shell: bash
        run: |
          set -euo pipefail
          python -c "
          import json, os, sys

          receipt_path = os.path.join(os.environ['RUNNER_TEMP'], 'state_gate_receipt.json')
          if not os.path.exists(receipt_path):
              print('ERROR: receipt file missing', file=sys.stderr)
              sys.exit(1)

          with open(receipt_path) as f:
              receipt = json.load(f)

          if not isinstance(receipt, dict):
              print('ERROR: receipt not a JSON object', file=sys.stderr)
              sys.exit(1)

          final_gate_result = receipt.get('final_gate_result', '')
          selected_mode = receipt.get('selected_mode', '')
          authority_result = receipt.get('authority_result', '')
          candidate_tests_result = receipt.get('candidate_tests_result', '')
          trusted_verifier_tree = receipt.get('trusted_verifier_tree_sha', '')
          trusted_base = receipt.get('trusted_base_sha', '')
          changed_paths_sha = receipt.get('changed_paths_sha256', '')
          authority_revision = receipt.get('authority_revision', '')
          content_sha = receipt.get('content_sha256', '')

          errors = []

          # final_gate_result must be PASS
          if final_gate_result != 'PASS':
              errors.append(f'final_gate_result={final_gate_result} (expected PASS)')

          # selected_mode must be supported
          if selected_mode not in {'transition', 'path_a_r1', 'legacy'}:
              errors.append(f'unsupported selected_mode={selected_mode!r}')

          # authority_result must be SUCCESS
          if authority_result != 'SUCCESS':
              errors.append(f'authority_result={authority_result} (expected SUCCESS)')

          # candidate_tests_result must be SUCCESS
          if candidate_tests_result != 'SUCCESS':
              errors.append(f'candidate_tests_result={candidate_tests_result} (expected SUCCESS)')

          # trusted verifier tree must equal trusted base
          if trusted_verifier_tree != trusted_base:
              errors.append(f'trusted_verifier_tree_sha={trusted_verifier_tree} != trusted_base_sha={trusted_base}')

          # changed_paths_sha256 must be lowercase SHA-256
          import re
          if not re.fullmatch(r'[0-9a-f]{64}', str(changed_paths_sha)):
              errors.append(f'changed_paths_sha256 format invalid: {changed_paths_sha!r}')

          # authority_revision must be nonempty
          if not str(authority_revision).strip():
              errors.append('authority_revision empty')

          # content_sha256 must be lowercase SHA-256
          if not re.fullmatch(r'[0-9a-f]{64}', str(content_sha)):
              errors.append(f'content_sha256 format invalid: {content_sha!r}')

          if errors:
              for e in errors:
                  print(f'RECEIPT_GATE_FAILURE: {e}', file=sys.stderr)
              sys.exit(1)

          print('RECEIPT_GATE_PASS: all checks passed')
          "

