# Owner Active-Authority Freeze

This file intentionally contains no executable authority.

When an exact-base execution authority is active, `owner/repository-modernization-v2-planning` must not receive ordinary Owner planning/documentation writes until that authority returns for audit. Any necessary planning change must either precede authority publication or explicitly supersede the active authority.
