# Manual Mode Console

Static fixture-only console for the manual-mode orchestrator MVP.

Open `index.html` directly in a browser. The demo uses local in-file fixture data and does not call a network endpoint, mutate `project_state`, run tools, dispatch runners, or invoke model APIs.
